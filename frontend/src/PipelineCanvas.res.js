// Generated by ReScript, PLEASE EDIT WITH CARE

import * as React from "react";
import * as Belt_Array from "@rescript/runtime/lib/es6/Belt_Array.js";
import * as PipelineModel from "./PipelineModel.res.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as JsxRuntime from "react/jsx-runtime";

function fmod(a, b) {
  return a - (a / b | 0) * b;
}

function snap(v) {
  return Math.round(v / 20.0) * 20.0;
}

function statusColor(status) {
  switch (status) {
    case "Idle" :
      return "#9e9e9e";
    case "Running" :
      return "#fdd835";
    case "Success" :
      return "#66bb6a";
    case "Failed" :
      return "#ef5350";
    case "Skipped" :
      return "#78909c";
  }
}

function connectionColor(fromPort) {
  switch (fromPort) {
    case "artifact" :
      return "#a5d6a7";
    case "attestation" :
      return "#ffcc80";
    case "metadata" :
      return "#ce93d8";
    case "output" :
      return "#90caf9";
    default:
      return "#b0bec5";
  }
}

function bezierPath(x1, y1, x2, y2) {
  let dx = Math.abs(x2 - x1) * 0.5;
  let cx1 = x1 + dx;
  let cx2 = x2 - dx;
  return `M ` + String(x1) + ` ` + String(y1) + ` C ` + String(cx1) + ` ` + String(y1) + ` ` + String(cx2) + ` ` + String(y2) + ` ` + String(x2) + ` ` + String(y2);
}

function inputPortPos(node) {
  return [
    node.x,
    node.y + node.height / 2.0
  ];
}

function outputPortPos(node) {
  return [
    node.x + node.width,
    node.y + node.height / 2.0
  ];
}

function findNode(nodes, id) {
  return Belt_Array.getBy(nodes, n => n.id === id);
}

function renderNode(node, isSelected, dispatch) {
  let color = PipelineModel.nodeColor(node.kind);
  let icon = PipelineModel.nodeIcon(node.kind);
  let kindLabel = PipelineModel.nodeKindToString(node.kind);
  let errorCount = node.validationErrors.length;
  let statusDot = statusColor(node.status);
  let borderStroke = isSelected ? "#ffffff" : "rgba(255,255,255,0.15)";
  let borderWidth = isSelected ? "2.5" : "1";
  let match = inputPortPos(node);
  let inY = match[1];
  let inX = match[0];
  let match$1 = outputPortPos(node);
  let outY = match$1[1];
  let outX = match$1[0];
  let match$2 = node.status;
  let tmp;
  switch (match$2) {
    case "Idle" :
      tmp = "idle";
      break;
    case "Running" :
      tmp = "running";
      break;
    case "Success" :
      tmp = "done";
      break;
    case "Failed" :
      tmp = "failed";
      break;
    case "Skipped" :
      tmp = "skipped";
      break;
  }
  return JsxRuntime.jsxs("g", {
    children: [
      JsxRuntime.jsx("rect", {
        height: String(node.height),
        width: String(node.width),
        fill: "rgba(0,0,0,0.25)",
        rx: "10",
        ry: "10",
        x: String(node.x + 2.0),
        y: String(node.y + 2.0)
      }),
      JsxRuntime.jsx("rect", {
        "aria-label": node.label + " (" + kindLabel + ")",
        role: "graphics-symbol",
        height: String(node.height),
        width: String(node.width),
        fill: "#1e1e2e",
        rx: "10",
        ry: "10",
        stroke: borderStroke,
        strokeWidth: borderWidth,
        x: String(node.x),
        y: String(node.y)
      }),
      JsxRuntime.jsx("rect", {
        height: "4",
        width: String(node.width),
        fill: color,
        rx: "10",
        ry: "10",
        x: String(node.x),
        y: String(node.y)
      }),
      JsxRuntime.jsx("rect", {
        height: "2",
        width: String(node.width),
        fill: color,
        x: String(node.x),
        y: String(node.y + 2.0)
      }),
      JsxRuntime.jsx("text", {
        children: icon,
        style: {
          fontSize: "16px",
          pointerEvents: "none"
        },
        fill: color,
        x: String(node.x + 12.0),
        y: String(node.y + 28.0)
      }),
      JsxRuntime.jsx("text", {
        children: node.label,
        style: {
          fontSize: "13px",
          fontWeight: "600",
          pointerEvents: "none"
        },
        fill: "#e0e0e0",
        x: String(node.x + 36.0),
        y: String(node.y + 30.0)
      }),
      JsxRuntime.jsx("text", {
        children: kindLabel,
        style: {
          fontSize: "10px",
          pointerEvents: "none"
        },
        fill: "#9e9e9e",
        x: String(node.x + 36.0),
        y: String(node.y + 48.0)
      }),
      JsxRuntime.jsx("circle", {
        cx: String(node.x + 16.0),
        cy: String(node.y + node.height - 14.0),
        fill: statusDot,
        r: "5"
      }),
      JsxRuntime.jsx("text", {
        children: tmp,
        style: {
          fontSize: "9px",
          pointerEvents: "none"
        },
        fill: "#9e9e9e",
        x: String(node.x + 26.0),
        y: String(node.y + node.height - 10.0)
      }),
      errorCount > 0 ? JsxRuntime.jsxs(JsxRuntime.Fragment, {
          children: [
            JsxRuntime.jsx("circle", {
              cx: String(node.x + node.width - 12.0),
              cy: String(node.y + 12.0),
              fill: "#ef5350",
              r: "9"
            }),
            JsxRuntime.jsx("text", {
              children: String(errorCount),
              style: {
                fontSize: "10px",
                fontWeight: "700",
                pointerEvents: "none"
              },
              fill: "#ffffff",
              textAnchor: "middle",
              x: String(node.x + node.width - 12.0),
              y: String(node.y + 16.0)
            })
          ]
        }) : null,
      JsxRuntime.jsx("circle", {
        "aria-label": node.label + " input port",
        role: "graphics-symbol",
        cx: String(inX),
        cy: String(inY),
        fill: "#37474f",
        r: String(7.0),
        stroke: "#90a4ae",
        strokeWidth: "1.5"
      }),
      JsxRuntime.jsx("circle", {
        style: {
          cursor: "crosshair"
        },
        onMouseUp: e => {
          e.stopPropagation();
          dispatch({
            TAG: "EndConnect",
            _0: node.id,
            _1: "input"
          });
        },
        cx: String(inX),
        cy: String(inY),
        fill: "transparent",
        r: String(14.0)
      }),
      JsxRuntime.jsx("circle", {
        "aria-label": node.label + " output port",
        role: "graphics-symbol",
        cx: String(outX),
        cy: String(outY),
        fill: color,
        r: String(7.0),
        stroke: "#ffffff",
        strokeWidth: "1.5"
      }),
      JsxRuntime.jsx("circle", {
        style: {
          cursor: "crosshair"
        },
        onMouseDown: e => {
          e.stopPropagation();
          let mx = e.clientX;
          let my = e.clientY;
          dispatch({
            TAG: "StartConnect",
            _0: node.id,
            _1: "output",
            _2: mx,
            _3: my
          });
        },
        cx: String(outX),
        cy: String(outY),
        fill: "transparent",
        r: String(14.0)
      })
    ],
    style: {
      cursor: "grab"
    },
    onClick: e => {
      e.stopPropagation();
      dispatch({
        TAG: "SelectNode",
        _0: node.id
      });
    },
    onDoubleClick: e => {
      e.stopPropagation();
      dispatch({
        TAG: "SelectNode",
        _0: node.id
      });
    },
    onMouseDown: e => {
      e.stopPropagation();
      let svgEl = e.currentTarget;
      svgEl.ownerSVGElement.getBoundingClientRect();
      dispatch({
        TAG: "StartDrag",
        _0: node.id,
        _1: e.clientX - node.x,
        _2: e.clientY - node.y
      });
    }
  }, node.id);
}

function renderConnection(conn, nodes, isSelected) {
  let fromNode = findNode(nodes, conn.fromNode);
  let toNode = findNode(nodes, conn.toNode);
  if (fromNode === undefined) {
    return null;
  }
  if (toNode === undefined) {
    return null;
  }
  let match = outputPortPos(fromNode);
  let match$1 = inputPortPos(toNode);
  let y2 = match$1[1];
  let x2 = match$1[0];
  let pathD = bezierPath(match[0], match[1], x2, y2);
  let color = connectionColor(conn.fromPort);
  let strokeW = isSelected ? "3" : "2";
  let opacity = isSelected ? "1" : "0.7";
  return JsxRuntime.jsxs("g", {
    children: [
      JsxRuntime.jsx("path", {
        style: {
          cursor: "pointer"
        },
        d: pathD,
        fill: "none",
        stroke: "transparent",
        strokeWidth: "12"
      }),
      JsxRuntime.jsx("path", {
        "aria-label": `Connection from ` + conn.fromNode + ` to ` + conn.toNode,
        role: "graphics-symbol",
        d: pathD,
        fill: "none",
        opacity: opacity,
        stroke: color,
        strokeLinecap: "round",
        strokeWidth: strokeW
      }),
      JsxRuntime.jsx("circle", {
        cx: String(x2 - 3.0),
        cy: String(y2),
        fill: color,
        opacity: opacity,
        r: "3"
      })
    ]
  }, conn.id);
}

function renderConnectPreview(connectState, nodes) {
  let fromNode = findNode(nodes, connectState.fromNode);
  if (fromNode === undefined) {
    return null;
  }
  let match = outputPortPos(fromNode);
  let pathD = bezierPath(match[0], match[1], connectState.mouseX, connectState.mouseY);
  return JsxRuntime.jsx("path", {
    d: pathD,
    fill: "none",
    opacity: "0.6",
    stroke: "#90caf9",
    strokeDasharray: "6,4",
    strokeLinecap: "round",
    strokeWidth: "2"
  });
}

function renderMinimap(nodes, connections, zoom, panX, panY, canvasWidth, canvasHeight) {
  if (nodes.length === 0) {
    return null;
  }
  let minX = {
    contents: 99999.0
  };
  let minY = {
    contents: 99999.0
  };
  let maxX = {
    contents: -99999.0
  };
  let maxY = {
    contents: -99999.0
  };
  Belt_Array.forEach(nodes, n => {
    if (n.x < minX.contents) {
      minX.contents = n.x;
    }
    if (n.y < minY.contents) {
      minY.contents = n.y;
    }
    if (n.x + n.width > maxX.contents) {
      maxX.contents = n.x + n.width;
    }
    if (n.y + n.height > maxY.contents) {
      maxY.contents = n.y + n.height;
      return;
    }
  });
  let bboxW = maxX.contents - minX.contents + 80.0;
  let bboxH = maxY.contents - minY.contents + 80.0;
  let scaleX = (180.0 - 20.0) / bboxW;
  let scaleY = (120.0 - 20.0) / bboxH;
  let mmScale = Math.min(scaleX, scaleY);
  let offsetX = minX.contents - 40.0;
  let offsetY = minY.contents - 40.0;
  let vpX = (- panX / zoom - offsetX) * mmScale + 10.0;
  let vpY = (- panY / zoom - offsetY) * mmScale + 10.0;
  let vpW = canvasWidth / zoom * mmScale;
  let vpH = canvasHeight / zoom * mmScale;
  return JsxRuntime.jsxs("g", {
    children: [
      JsxRuntime.jsx("rect", {
        height: String(120.0),
        width: String(180.0),
        fill: "rgba(20,20,30,0.85)",
        rx: "6",
        ry: "6",
        stroke: "rgba(255,255,255,0.1)",
        strokeWidth: "1",
        x: "0",
        y: "0"
      }),
      Belt_Array.map(nodes, n => {
        let nx = (n.x - offsetX) * mmScale + 10.0;
        let ny = (n.y - offsetY) * mmScale + 10.0;
        let nw = n.width * mmScale;
        let nh = n.height * mmScale;
        return JsxRuntime.jsx("rect", {
          height: String(Math.max(nh, 2.0)),
          width: String(Math.max(nw, 3.0)),
          fill: PipelineModel.nodeColor(n.kind),
          opacity: "0.8",
          rx: "1",
          x: String(nx),
          y: String(ny)
        }, "mm-" + n.id);
      }),
      Belt_Array.map(connections, conn => {
        let fromN = findNode(nodes, conn.fromNode);
        let toN = findNode(nodes, conn.toNode);
        if (fromN === undefined) {
          return null;
        }
        if (toN === undefined) {
          return null;
        }
        let sx = (fromN.x + fromN.width - offsetX) * mmScale + 10.0;
        let sy = (fromN.y + fromN.height / 2.0 - offsetY) * mmScale + 10.0;
        let dx = (toN.x - offsetX) * mmScale + 10.0;
        let dy = (toN.y + toN.height / 2.0 - offsetY) * mmScale + 10.0;
        return JsxRuntime.jsx("line", {
          stroke: "rgba(144,202,249,0.4)",
          strokeWidth: "1",
          x1: String(sx),
          x2: String(dx),
          y1: String(sy),
          y2: String(dy)
        }, "mmc-" + conn.id);
      }),
      JsxRuntime.jsx("rect", {
        height: String(vpH),
        width: String(vpW),
        fill: "none",
        stroke: "rgba(255,255,255,0.4)",
        strokeDasharray: "3,2",
        strokeWidth: "1",
        x: String(vpX),
        y: String(vpY)
      })
    ]
  });
}

function renderContextMenu(screenX, screenY, dispatch) {
  let menuItemStyle = {
    background: "transparent",
    color: "#e0e0e0",
    cursor: "pointer",
    fontSize: "12px",
    padding: "6px 16px"
  };
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsx("div", {
        children: "Add Source Image",
        style: menuItemStyle,
        onClick: param => {
          let pos = {
            x: screenX,
            y: screenY
          };
          dispatch({
            TAG: "AddNode",
            _0: {
              TAG: "Source",
              imageRef: "",
              tag: "latest"
            },
            _1: pos
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Add Build Step",
        style: menuItemStyle,
        onClick: param => {
          let pos = {
            x: screenX,
            y: screenY
          };
          dispatch({
            TAG: "AddNode",
            _0: {
              TAG: "BuildStep",
              command: "",
              layer: 0
            },
            _1: pos
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Add Security Gate",
        style: menuItemStyle,
        onClick: param => {
          let pos = {
            x: screenX,
            y: screenY
          };
          dispatch({
            TAG: "AddNode",
            _0: {
              TAG: "SecurityGate",
              policy: "strict",
              mode: "enforce"
            },
            _1: pos
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Add Sign Step",
        style: menuItemStyle,
        onClick: param => {
          let pos = {
            x: screenX,
            y: screenY
          };
          dispatch({
            TAG: "AddNode",
            _0: {
              TAG: "SignStep",
              keyId: "default",
              transparency: true
            },
            _1: pos
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Add Push",
        style: menuItemStyle,
        onClick: param => {
          let pos = {
            x: screenX,
            y: screenY
          };
          dispatch({
            TAG: "AddNode",
            _0: {
              TAG: "Push",
              registry: "",
              repository: ""
            },
            _1: pos
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        style: {
          background: "rgba(255,255,255,0.1)",
          height: "1px",
          margin: "4px 0"
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Duplicate Selected",
        style: menuItemStyle,
        onClick: param => {
          dispatch({
            TAG: "DuplicateNode",
            _0: ""
          });
          dispatch("CloseContextMenu");
        }
      }),
      JsxRuntime.jsx("div", {
        children: "Delete Selected",
        style: {
          background: "transparent",
          color: "#ef5350",
          cursor: "pointer",
          fontSize: "12px",
          padding: "6px 16px"
        },
        onClick: param => {
          dispatch("DeleteSelected");
          dispatch("CloseContextMenu");
        }
      })
    ],
    style: {
      background: "#2a2a3e",
      borderRadius: "6px",
      boxShadow: "0 4px 16px rgba(0,0,0,0.4)",
      left: String(screenX) + "px",
      minWidth: "160px",
      padding: "4px 0",
      position: "fixed",
      top: String(screenY) + "px",
      zIndex: "2000"
    },
    onClick: e => {
      e.stopPropagation();
    }
  });
}

function PipelineCanvas(props) {
  let dispatch = props.dispatch;
  let pipeline = props.pipeline;
  let containerRef = React.useRef(null);
  let match = React.useState(() => [
    1200.0,
    800.0
  ]);
  let setCanvasSize = match[1];
  let canvasSize = match[0];
  let match$1 = React.useState(() => {});
  let setContextMenu = match$1[1];
  let contextMenu = match$1[0];
  React.useEffect(() => {
    let measure = () => {
      let el = containerRef.current;
      if (el == null) {
        return;
      }
      let rect = el.getBoundingClientRect();
      setCanvasSize(param => [
        rect.width,
        rect.height
      ]);
    };
    measure();
    let addResizeListener = (function(fn) { window.addEventListener("resize", fn) });
    let removeResizeListener = (function(fn) { window.removeEventListener("resize", fn) });
    addResizeListener(measure);
    return () => removeResizeListener(measure);
  }, []);
  React.useEffect(() => {
    let handler = e => {
      let key = e.key;
      let ctrlKey = e.ctrlKey;
      let metaKey = e.metaKey;
      switch (key) {
        case "Backspace" :
        case "Delete" :
          return dispatch("DeleteSelected");
        case "Escape" :
          dispatch("CancelConnect");
          dispatch("CloseContextMenu");
          return setContextMenu(param => {});
        case "a" :
          if (ctrlKey || metaKey) {
            e.preventDefault();
            return dispatch("SelectAll");
          } else {
            return;
          }
        case "z" :
          if (ctrlKey || metaKey) {
            return dispatch("Undo");
          } else {
            return;
          }
        default:
          return;
      }
    };
    let addKeyListener = (function(fn) { window.addEventListener("keydown", fn) });
    let removeKeyListener = (function(fn) { window.removeEventListener("keydown", fn) });
    addKeyListener(handler);
    return () => removeKeyListener(handler);
  }, []);
  let handleMouseMove = e => {
    let mx = e.clientX;
    let my = e.clientY;
    let worldX = (mx - pipeline.panX) / pipeline.zoom;
    let worldY = (my - pipeline.panY) / pipeline.zoom;
    dispatch({
      TAG: "UpdateDrag",
      _0: snap(worldX),
      _1: snap(worldY)
    });
    dispatch({
      TAG: "UpdateConnect",
      _0: worldX,
      _1: worldY
    });
  };
  let handleMouseUp = _e => {
    dispatch("EndDrag");
    dispatch("CancelConnect");
  };
  let handleCanvasClick = _e => {
    dispatch({
      TAG: "SelectNode",
      _0: undefined
    });
    dispatch({
      TAG: "SelectConnection",
      _0: undefined
    });
    setContextMenu(param => {});
  };
  let handleWheel = e => {
    e.preventDefault();
    let delta = e.deltaY;
    let direction = delta > 0.0 ? -1.0 : 1.0;
    let newZoom = Math.max(0.25, Math.min(3.0, pipeline.zoom + direction * 0.1));
    dispatch({
      TAG: "SetZoom",
      _0: newZoom
    });
  };
  let handleContextMenu = e => {
    e.preventDefault();
    let sx = e.clientX;
    let sy = e.clientY;
    setContextMenu(param => [
      sx,
      sy
    ]);
  };
  let handleMouseDown = e => {
    let button = e.button;
    let altKey = e.altKey;
    if (button === 1 || altKey) {
      e.preventDefault();
      return;
    }
  };
  let canvasH = canvasSize[1];
  let canvasW = canvasSize[0];
  return JsxRuntime.jsxs("div", {
    children: [
      JsxRuntime.jsxs("svg", {
        children: [
          JsxRuntime.jsxs("defs", {
            children: [
              JsxRuntime.jsx("pattern", {
                children: JsxRuntime.jsx("circle", {
                  cx: "1",
                  cy: "1",
                  fill: "rgba(255,255,255,0.06)",
                  r: "0.5"
                }),
                id: "pipeline-grid",
                height: String(20.0),
                width: String(20.0),
                patternTransform: `translate(` + String(fmod(pipeline.panX, 20.0 * pipeline.zoom)) + ` ` + String(fmod(pipeline.panY, 20.0 * pipeline.zoom)) + `) scale(` + String(pipeline.zoom) + `)`,
                patternUnits: "userSpaceOnUse"
              }),
              JsxRuntime.jsxs("pattern", {
                children: [
                  JsxRuntime.jsx("line", {
                    stroke: "rgba(255,255,255,0.03)",
                    strokeWidth: "0.5",
                    x1: "0",
                    x2: String(20.0 * 5.0),
                    y1: "0",
                    y2: "0"
                  }),
                  JsxRuntime.jsx("line", {
                    stroke: "rgba(255,255,255,0.03)",
                    strokeWidth: "0.5",
                    x1: "0",
                    x2: "0",
                    y1: "0",
                    y2: String(20.0 * 5.0)
                  })
                ],
                id: "pipeline-grid-major",
                height: String(20.0 * 5.0),
                width: String(20.0 * 5.0),
                patternTransform: `translate(` + String(fmod(pipeline.panX, 20.0 * 5.0 * pipeline.zoom)) + ` ` + String(fmod(pipeline.panY, 20.0 * 5.0 * pipeline.zoom)) + `) scale(` + String(pipeline.zoom) + `)`,
                patternUnits: "userSpaceOnUse"
              }),
              JsxRuntime.jsx("filter", {
                children: JsxRuntime.jsx("feDropShadow", {
                  dx: "1",
                  dy: "2",
                  floodColor: "#000000",
                  floodOpacity: "0.3",
                  stdDeviation: "3"
                }),
                id: "node-shadow",
                height: "130%",
                width: "130%",
                x: "-10%",
                y: "-10%"
              })
            ]
          }),
          JsxRuntime.jsx("rect", {
            height: "100%",
            width: "100%",
            fill: "#14141e"
          }),
          JsxRuntime.jsx("rect", {
            height: "100%",
            width: "100%",
            fill: "url(#pipeline-grid-major)"
          }),
          JsxRuntime.jsx("rect", {
            height: "100%",
            width: "100%",
            fill: "url(#pipeline-grid)"
          }),
          JsxRuntime.jsxs("g", {
            children: [
              Belt_Array.map(pipeline.connections, conn => {
                let isSelected = pipeline.selectedConnection === conn.id;
                return renderConnection(conn, pipeline.nodes, isSelected);
              }),
              Belt_Array.map(pipeline.nodes, node => {
                let isSelected = pipeline.selectedNode === node.id;
                return renderNode(node, isSelected, dispatch);
              })
            ],
            transform: `translate(` + String(pipeline.panX) + `, ` + String(pipeline.panY) + `) scale(` + String(pipeline.zoom) + `)`
          }),
          JsxRuntime.jsx("g", {
            children: renderMinimap(pipeline.nodes, pipeline.connections, pipeline.zoom, pipeline.panX, pipeline.panY, canvasW, canvasH),
            transform: `translate(` + String(canvasW - 180.0 - 12.0) + `, ` + String(canvasH - 120.0 - 12.0) + `)`
          }),
          JsxRuntime.jsx("text", {
            children: String(pipeline.zoom * 100.0 | 0) + "%",
            style: {
              fontSize: "11px",
              pointerEvents: "none"
            },
            fill: "rgba(255,255,255,0.3)",
            textAnchor: "end",
            x: String(canvasW - 16.0),
            y: "24"
          })
        ],
        "aria-label": "Container assembly pipeline graph",
        role: "graphics-document",
        style: {
          display: "block"
        },
        height: "100%",
        width: "100%",
        onClick: handleCanvasClick,
        onContextMenu: handleContextMenu,
        onMouseDown: handleMouseDown,
        onMouseMove: handleMouseMove,
        onMouseUp: handleMouseUp,
        onWheel: handleWheel
      }),
      contextMenu !== undefined ? renderContextMenu(contextMenu[0], contextMenu[1], dispatch) : null,
      JsxRuntime.jsxs("div", {
        children: [
          JsxRuntime.jsx("button", {
            children: "+",
            "aria-label": "Zoom in",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#e0e0e0",
              cursor: "pointer",
              fontSize: "14px",
              fontWeight: "600",
              padding: "4px 10px"
            },
            title: "Zoom in",
            onClick: param => dispatch({
              TAG: "SetZoom",
              _0: Math.min(3.0, pipeline.zoom + 0.1)
            })
          }),
          JsxRuntime.jsx("button", {
            children: "-",
            "aria-label": "Zoom out",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#e0e0e0",
              cursor: "pointer",
              fontSize: "14px",
              fontWeight: "600",
              padding: "4px 10px"
            },
            title: "Zoom out",
            onClick: param => dispatch({
              TAG: "SetZoom",
              _0: Math.max(0.25, pipeline.zoom - 0.1)
            })
          }),
          JsxRuntime.jsx("button", {
            children: "1:1",
            "aria-label": "Reset viewport",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#e0e0e0",
              cursor: "pointer",
              fontSize: "12px",
              padding: "4px 10px"
            },
            title: "Reset zoom and pan",
            onClick: param => {
              dispatch({
                TAG: "SetZoom",
                _0: 1.0
              });
              dispatch({
                TAG: "SetPan",
                _0: 0.0,
                _1: 0.0
              });
            }
          }),
          JsxRuntime.jsx("div", {
            style: {
              background: "rgba(255,255,255,0.1)",
              width: "1px"
            }
          }),
          JsxRuntime.jsx("button", {
            children: "Undo",
            "aria-label": "Undo (Ctrl+Z)",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#e0e0e0",
              cursor: "pointer",
              fontSize: "12px",
              padding: "4px 10px"
            },
            title: "Undo",
            onClick: param => dispatch("Undo")
          }),
          JsxRuntime.jsx("div", {
            style: {
              background: "rgba(255,255,255,0.1)",
              width: "1px"
            }
          }),
          JsxRuntime.jsx("button", {
            children: "All",
            "aria-label": "Select all nodes (Ctrl+A)",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#e0e0e0",
              cursor: "pointer",
              fontSize: "12px",
              padding: "4px 10px"
            },
            title: "Select all",
            onClick: param => dispatch("SelectAll")
          }),
          JsxRuntime.jsx("button", {
            children: "Del",
            "aria-label": "Delete selected (Del)",
            style: {
              background: "#2a2a3e",
              border: "none",
              borderRadius: "4px",
              color: "#ef5350",
              cursor: "pointer",
              fontSize: "12px",
              padding: "4px 10px"
            },
            title: "Delete",
            onClick: param => dispatch("DeleteSelected")
          })
        ],
        "aria-label": "Pipeline canvas toolbar",
        role: "toolbar",
        style: {
          background: "rgba(30,30,46,0.9)",
          borderRadius: "6px",
          display: "flex",
          gap: "4px",
          left: "10px",
          padding: "4px",
          position: "absolute",
          top: "10px",
          zIndex: "100"
        }
      }),
      JsxRuntime.jsx("div", {
        children: pipeline.name,
        style: {
          color: "rgba(255,255,255,0.5)",
          fontSize: "13px",
          left: "50%",
          pointerEvents: "none",
          position: "absolute",
          top: "12px",
          transform: "translateX(-50%)"
        }
      }),
      JsxRuntime.jsx("div", {
        children: String(pipeline.nodes.length) + " nodes, " + String(pipeline.connections.length) + " connections",
        style: {
          bottom: "12px",
          color: "rgba(255,255,255,0.3)",
          fontSize: "11px",
          left: "12px",
          pointerEvents: "none",
          position: "absolute"
        }
      })
    ],
    ref: Primitive_option.some(containerRef),
    "aria-label": "Pipeline assembly canvas",
    role: "main",
    style: {
      background: "#14141e",
      height: "100%",
      overflow: "hidden",
      position: "relative",
      width: "100%"
    }
  });
}

let gridSize = 20.0;

let nodeDefaultWidth = 180.0;

let nodeDefaultHeight = 80.0;

let portRadius = 7.0;

let portHitRadius = 14.0;

let minimapWidth = 180.0;

let minimapHeight = 120.0;

let minimapPadding = 12.0;

let zoomMin = 0.25;

let zoomMax = 3.0;

let zoomStep = 0.1;

let make = PipelineCanvas;

export {
  gridSize,
  nodeDefaultWidth,
  nodeDefaultHeight,
  portRadius,
  portHitRadius,
  minimapWidth,
  minimapHeight,
  minimapPadding,
  zoomMin,
  zoomMax,
  zoomStep,
  fmod,
  snap,
  statusColor,
  connectionColor,
  bezierPath,
  inputPortPos,
  outputPortPos,
  findNode,
  renderNode,
  renderConnection,
  renderConnectPreview,
  renderMinimap,
  renderContextMenu,
  make,
}
/* react Not a pure module */
