#!/bin/sh
# SPDX-License-Identifier: PMPL-1.0-or-later
# Generated by alloyiser — Alloy analysis script
# Requires: java, alloy.jar

ALLOY_JAR="alloy.jar"
MODEL="/var/mnt/eclipse/repos/fleet-ecosystem/stapeln/generated/alloyiser/stapeln.als"
SOLVER="sat4j"
TIMEOUT="300"

echo "=== alloyiser analysis ==="
echo "Model: $MODEL"
echo "Solver: $SOLVER"
echo "Assertions: 3"

echo "Checking: no_orphan_records..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "no_orphan_records"
echo ""

echo "Checking: container_integrity..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "container_integrity"
echo ""

echo "Checking: layer_acyclic..."
timeout "$TIMEOUT" java -jar "$ALLOY_JAR" -batch -solver "$SOLVER" "$MODEL" 2>&1 | grep -A5 "layer_acyclic"
echo ""

echo "=== analysis complete ==="
