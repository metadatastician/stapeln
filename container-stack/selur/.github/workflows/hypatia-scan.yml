# SPDX-License-Identifier: MPL-2.0
# Hypatia neurosymbolic security scanning for selur
name: Hypatia Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: '30 5 * * 1'  # Weekly Monday 05:30 UTC

permissions: read-all

jobs:
  hypatia-scan:
    name: Neurosymbolic Security Scan
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5
        with:
          fetch-depth: 0

      - name: Run Hypatia scan
        uses: hyperpolymath/hypatia-action@main
        with:
          scan-depth: full
          report-format: sarif
          fail-on-critical: true

      - name: Upload SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@b2f9ef845756500b97acbdaf5c1dd4e9c1d15734  # v3
        with:
          sarif_file: hypatia-results.sarif
        continue-on-error: true
